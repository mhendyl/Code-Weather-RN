import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingVertical: 20,
    backgroundColor: 'rgba(97, 99, 98, .4)',
    padding: 10,
    borderRadius: 10,
    marginTop: 30,
  },
  wrapperWind: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  firstWrap: {
    marginBottom: 10,
  },
  textStyling: {
    justifyContent: 'space-between',
    fontSize: 10,
  },
});

export default styles;
